<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:local="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <EditText
        android:id="@+id/login_edit_text"
        android:inputType="text"
        android:singleLine="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        local:layout_constraintBottom_toTopOf="@id/password_edit_text"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:textColor="@color/primaryTextColor"
        local:MvxBind="Text Login"/>

    <EditText
        android:id="@+id/password_edit_text"
        android:singleLine="true"
        android:inputType="textPassword"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:layout_marginTop="10dp"
        android:textColor="@color/primaryTextColor"
        local:layout_constraintBottom_toTopOf="@id/confirm_password_edit_text"
        local:layout_constraintRight_toRightOf="parent"
        local:layout_constraintLeft_toLeftOf="parent"
        local:MvxBind="Text Password"/>

    <EditText
        android:id="@+id/confirm_password_edit_text"
        android:singleLine="true"
        android:inputType="textPassword"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:layout_marginTop="10dp"
        android:textColor="@color/primaryTextColor"
        local:layout_constraintTop_toTopOf="parent"
        local:layout_constraintBottom_toBottomOf="parent"
        local:layout_constraintRight_toRightOf="parent"
        local:layout_constraintLeft_toLeftOf="parent"
        local:MvxBind="Text ConfirmPassword"/>

    <Button
        android:id="@+id/sign_up_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:layout_marginBottom="80dp"
        android:backgroundTint="@android:color/holo_orange_dark"
        local:layout_constraintBottom_toBottomOf="parent"
        local:MvxBind="Click SignUpButtonTappedCommand"/>

</androidx.constraintlayout.widget.ConstraintLayout>
